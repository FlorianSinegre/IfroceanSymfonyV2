{% extends 'base.html.twig' %}

{% block title %}Plage{% endblock %}

{% block body %}
    <h1>Plage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ plage.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ plage.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('plage_index') }}">back to list</a>

    <a href="{{ path('plage_edit', {'id': plage.id}) }}">edit</a>

    {{ include('plage/_delete_form.html.twig') }}
{% endblock %}
