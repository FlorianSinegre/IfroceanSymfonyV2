<form method="post" action="{{ path('zone_de_prelevement_delete', {'id': zone_de_prelevement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ zone_de_prelevement.id) }}">
    <button class="btn">Delete</button>
</form>
